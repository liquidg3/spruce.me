<div class="<%= className %>">

    <div class="form-group">

        <label for="<%= name %>" class="property-label"><%= options.label %></label>
        <input type="file" name="<%= name %>">

        <% if(value) { %>
            <a href="<%=value.public%>"><%=value.filename%></a>
        <% } %>

    </div>

</div>